# PixivCrawler 工具说明

PixivCrawler 是用于个人本地归档的 Windows 桌面工具，只下载用户登录后在 Pixiv
中本来就能正常查看的作品，不绕过验证码、年龄或地区限制，也不提供重发布服务。

## 首版功能

- 输入一个或多个作品链接/ID、按主题或标签搜索作品、输入作者链接/ID，或归档当前账号收藏。
- 搜索支持关键词和 Pixiv 标签页链接，可设置最低收藏数、浏览数、点赞数，以及 1–100 张图片的下载上限。
- 搜索会通过 Pixiv 官方候选接口解析精确匹配的翻译标签，例如将“莉可丽丝”解析为“リコリス・リコイル”。
- 为控制站点请求量，启用热度门槛时最多检查 500 个最新候选作品，并优先使用搜索响应中的统计数据做预筛选。
- 搜索解析显示“已检查 X/500、匹配 Y 张”，以 4 并发读取详情，只有符合筛选的作品才请求图片页。
- 支持插画、漫画、多图和 ugoira；ugoira 保留原始 ZIP、帧时序 JSON，并生成 MP4。
- 支持日期、标签、作品类型、AI 标记、年龄分级和收藏可见性筛选。
- 任务可以暂停、继续、取消、失败重试和在应用重启后恢复。
- 使用独立的内置 Pixiv 登录窗口，不读取外部浏览器 Cookie。
- 默认最多 4 个在途请求、每 500ms 启动一个请求；遇到 429 时全局冷却并遵循 Retry-After。
- 支持系统代理和自定义 HTTP/HTTPS/SOCKS5 代理。
- 使用 SQLite 做任务记录与去重，每个作品同时保存可独立阅读的 `artwork.json`。

## 默认存储

下载目录为 `图片/PixivCrawler/{作者名}_{作者ID}/{标题}_{作品ID}/`，图片名为
`{作品ID}_p{页码}.{扩展名}`。安装版应用数据位于 `%LOCALAPPDATA%/PixivCrawler`；
便携版数据位于程序旁的 `data` 目录。

## 使用边界

用户应遵守 Pixiv 服务条款、作者权利和当地法律。工具不会尝试下载已删除、无权查看
或由站点访问控制阻止的内容。站点内部接口可能变化，应用会把结构变化报告为适配器错误。
